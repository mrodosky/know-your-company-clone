- @companies.each do |company|
  %li
    = link_to company.name, admin_company_path(company)

= link_to "Add Company to Database", new_admin_company_path

= link_to "Sign Out", destroy_user_session_path, :method => :delete